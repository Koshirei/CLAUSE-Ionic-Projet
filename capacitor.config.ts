import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CLAUSE-Ionic-Projet',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
